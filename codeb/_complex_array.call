extern long f(void*);

long a[] = {1,1,1};


long result=f(a);    
long expected = 4 + 7;

if (result != expected) {
    printf("expected: %ld, got: %ld\n",expected,result);
    return 1==0;
}

return 1==1;
